Python wrapper on a C/C++ library that obtains a list of  mounted removable volumes on a Linux system.

It returns: mount path, device name, device format and device label.

Supports ISO9660 and UDF formats.